{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd emtech_backend && python manage.py migrate && gunicorn emtech_backend.wsgi:application --bind 0.0.0.0:$PORT"
  }
} 